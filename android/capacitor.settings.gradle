// DO NOT edit this file by hand — it is regenerated by `npx cap sync
// android` (or `npx cap update android`) based on the Capacitor
// plugins actually installed in package.json. It is checked in only
// so a fresh checkout has a valid Gradle project BEFORE the CI
// workflow's first `cap sync` step runs (see
// .github/workflows/android-build.yml). Right after that step, this
// file's real, up-to-date content replaces what's here.
include ':capacitor-app'
project(':capacitor-app').projectDir = new File('../node_modules/@capacitor/app/android')
