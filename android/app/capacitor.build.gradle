// DO NOT edit this file by hand — regenerated by `npx cap sync
// android` from the Capacitor plugins in package.json. Checked in
// only so the app module resolves on a fresh checkout before CI's
// first `cap sync` step runs and overwrites it with the real
// dependency list.
apply plugin: 'com.android.application'

dependencies {
    implementation project(':capacitor-app')
}

if (hasProperty('postBuildExtras')) {
    postBuildExtras()
}
