# Add project specific ProGuard rules here.
# minifyEnabled is false by default (see app/build.gradle) so this
# file has no effect yet — kept present so enabling minification later
# is a one-line change, not a missing-file build failure.
